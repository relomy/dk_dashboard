# Commit messages

Use this format for commits:

```text
type(scope): short summary
```

Use a lowercase type such as `feat`, `fix`, `test`, `docs`, or `chore`. Keep the summary imperative and concise.
