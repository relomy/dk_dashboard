# Domain Docs

Before exploring, read `CONTEXT.md` at the repo root and relevant ADRs under `docs/adr/`. If they do not exist, proceed silently.

Use the glossary vocabulary from `CONTEXT.md` when naming domain concepts. If a needed concept is missing, note it for domain modeling. Flag conflicts with existing ADRs instead of silently overriding them.

This is a single-context repository:

```text
/
├── CONTEXT.md
├── docs/adr/
└── src/
```
